**Utilisateur**

<audio controls>
    <source src="/audio/chat.wav" type="audio/wav"/>
    Votre navigateur ne prend pas en charge l'élément audio.
</audio>

**Assistant**

Bien sûr, voici la transcription de ce que vous avez dit :
« Aujourd'hui, le 15 juillet 2025, Mistral AI annonce les nouveaux modèles Voxtral. »
Et voici le code Python pour générer les n premiers nombres de la suite de Fibonacci :
```python
def fibonacci(n):
    if n <= 0:
        return []
    elif n == 1:
        return [0]
    elif n == 2:
        return [0, 1]

fib_sequence = [0, 1]
for i in range(2, n):
    next_value = fib_sequence[-1] + fib_sequence[-2]
    fib_sequence.append(next_value)

return fib_sequence

# Exemple d'utilisation :
n = 10  # Modifiez cette valeur pour obtenir les n premiers nombres de la suite de Fibonacci
print(fibonacci(n))
```
Ce code définit une fonction `fibonacci` qui prend un entier `n` et renvoie une liste contenant les `n` premiers nombres de la suite de Fibonacci. Vous pouvez modifier la valeur de `n` pour obtenir le nombre souhaité de nombres de Fibonacci.
