import { ExplorerTabs, ExplorerTab } from '@/components/common/explorer-tabs';

import SingleTargetTab from './single_target_tab/_page.mdx';
import MultiTargetTab from './multi_target_tab/_page.mdx';

Data must be stored in JSON Lines (`.jsonl`) files, which allow storing multiple JSON objects, each on a new line.

We provide two endpoints:

- `v1/classifications`: To classify raw text.
- `v1/chat/classifications`: To classify chats and multi-turn interactions.

There are 2 main kinds of classification models\***\*:\*\***

- Single Target
- Multi-Target

<ExplorerTabs id="classification-models-types">
    <ExplorerTab value="single-target" label="Single Target">
        <SingleTargetTab/>
    </ExplorerTab>
    <ExplorerTab value="multi-Target" label="Multi-Target">
        <MultiTargetTab/>
    </ExplorerTab>
</ExplorerTabs>